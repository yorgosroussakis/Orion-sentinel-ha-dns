# Backup Configuration
TZ=UTC
BACKUP_RETENTION_DAYS=7

# Backup schedule: Daily at current time when started
# Backups are stored in ./backups/ directory
# Old backups are automatically cleaned up after BACKUP_RETENTION_DAYS
