# Signal CLI Configuration
This directory stores the signal-cli configuration including registered phone numbers and encryption keys. Do not delete this directory as it contains your Signal registration data.
